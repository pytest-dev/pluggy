[git]
url = "https://github.com/tox-dev/tox"
into = "tox"

[environment]
kind = "uv-venv"

[environment.install]
editables = [".[completion]", "../.."]

[environment.install.uv]
groups = ["test"]

[[test]]
argv = ["pytest"]
